<view class="button-container">
  <view class="button {{className}}" bindtap="onTap" data-value='{{value}}' data-index="{{index}}">
    <!-- <text ></text> -->
    <image src="{{icon}}" class="button-icon" mode=""/>
    <text class="button-text">{{text}}</text>
  </view>
</view>